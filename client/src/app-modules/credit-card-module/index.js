import CreditCard from "./pages/credit-card/credit-card";

export default {
  pages: [
    {
      routeProps: {
        path: "/credit-card",
        component: CreditCard,
      },
      name: "CreditCard",
    },
  ],
};
